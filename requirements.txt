# Analyses (CPU)
numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
openpyxl>=3.1
# Generation and NLI verification (GPU)
torch>=2.1
transformers>=4.44
huggingface_hub>=0.23
accelerate>=0.30
sentencepiece>=0.1.99
protobuf>=4.25
# The few-shot judges call an OpenAI-compatible server (e.g., vLLM), installed separately.
